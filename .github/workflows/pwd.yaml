jobs:
  my_job:
    runs-on: ubuntu-latest
    steps:
      - name: Capture Current Directory
        id: current_dir
        run: |
          echo "::set-output name=current_dir::$(pwd)"

      - name: Use Current Directory
        run: |
          echo "Current Directory: ${{ steps.current_dir.outputs.current_dir }}"
