{
  "name": "testfolio",
  "version": "0.0.1",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "ntli build -a",
    "dev": "ntli dev -c",
    "preview": "ntli preview",
    "generate-allure": "allure generate . --clean -o netlify_reports/allure-report",
    "build-allure": "npm install -g allure-commandline"
  },
  "dependencies": {
    "@netlify/sdk": "^1.0.9",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "allure-commandline": "*"
  }
}
